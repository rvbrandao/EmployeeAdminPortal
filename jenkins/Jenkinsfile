pipeline {
    agent any

    stages {
        
        stage('Build') {
           steps {
               mvn 'clean install'
                }
        }

        stage('Checkout Github') {
            steps {
                git branch: 'master', credentialsId: 'Github-Personal-access-tokens', url: 'https://github.com/rvbrandao/employeeadminportal.git'
            }
        }

        stage('Test') {
            steps {        
            }
        }
    }

    post {
        success {
            echo 'Pipeline completed successfully!'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}

